<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gamingsmlogin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GamingSMLogin"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity
            android:name=".SettingsMain"
            android:exported="false" />
        <activity
            android:name=".AccountSettingActivity"
            android:exported="false" />
        <activity android:name=".ViewUserForSearchActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".PlayerFeaturesActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".GameFilterDialogActivity" />
        <activity android:name=".GameSelectActivity" />
        <activity android:name=".SearchForDetailedActivity" />
        <activity android:name=".CommentPageActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".MainMessageActivity" />
        <activity android:name=".CrtMessageActivity" />
        <activity android:name=".SingleMessageChatActivity" />
        <activity
            android:name=".CrtAccountActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".LogSignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>